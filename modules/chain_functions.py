# modules/chain_functions.py

class Chain__select_good_search:
    @staticmethod
    def invoke(payload):
        # 실제 체인 호출 로직을 구현합니다.
        # 여기서는 예시로 payload를 그대로 반환합니다.
        return type('Response', (object,), {'content': "YES"})

class Chain__select_good_search_with_body:
    @staticmethod
    def invoke(payload):
        # 실제 체인 호출 로직을 구현합니다.
        # 여기서는 예시로 payload를 그대로 반환합니다.
        return type('Response', (object,), {'content': "YES"})
